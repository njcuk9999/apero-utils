# General settings (do not remove)
settings:
  instrument: NIRPS

# name:
#     apero profile: str, absolute path to APERO profile (settings)
#     lbl path: str, absolute path to LBL directory
#     username: str, username for html
#     account: str, account for html
#     skip obj table: bool: if True, skip making the object table
#     skip recipe table: bool: if True, skip making the recipe table
#     skip msg table: bool: if True, skip making the msg table
nirps ha V0.7.275 ref cal:
    apero profile: /nirps_raw/nirps/apero-data/drs-settings/setup_07254_nirps_ha
    lbl path: /space/spirou/LBL/NIRPS_HA
    skip obj table: False
    skip recipe table: True
    skip msg table: True

nirps ha V0.7.275 per night cal:
    apero profile: /nirps_raw/nirps/apero-data/drs-settings/setup_07254_nirpsha_pernight
    lbl path: /space/spirou/LBL/NIRPS_HA_pernight
    skip obj table: False
    skip recipe table: True
    skip msg table: True

nirps he V0.7.275 ref cal:
    apero profile: /nirps_raw/nirps/apero-data/drs-settings/setup_07254_nirps_he
    lbl path: /space/spirou/LBL/NIRPS_HE
    skip obj table: False
    skip recipe table: True
    skip msg table: True

nirps he V0.7.275 per night cal:
    apero profile: /nirps_raw/nirps/apero-data/drs-settings/setup_07254_nirpshe_pernight
    lbl path: /space/spirou/LBL/NIRPS_HE_pernight
    skip obj table: False
    skip recipe table: True
    skip msg table: True
