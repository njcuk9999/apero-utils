"""
Run the following chechs on Dark
- how many recipes were run?
- how many of each output do we have?
- using the log.fits how many passed all QC and which QC did they fail
- for each calib entry how many are in the calibDB?
- using the log.fits how many failed to finished and using the 'ERRORS' and 'LOGFILE' columns can you report why?
"""
